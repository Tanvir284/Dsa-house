const fs = require('fs');
const path = require('path');

const ARENA_PATH = path.join(__dirname, '..', 'src', 'data', 'problems_arena.json');

const EXPLANATIONS = {
  'lc-1': 'Use a hash map to store the complement of each number (target - nums[i]) and its index as we iterate. If the complement is already in the map, we have found the pair. Time complexity: O(n), Space complexity: O(n).',
  'cf-4A': 'An even number greater than 2 can always be divided into two even positive integers (e.g., w - 2 and 2). If w <= 2 or w is odd, it is impossible. Time complexity: O(1), Space complexity: O(1).',
  'cf-71A': 'If the word length is strictly greater than 10, replace it with the first character, the length of the middle characters (length - 2), and the last character. Otherwise, keep the word as is. Time complexity: O(1) per word, Space complexity: O(1).',
  'cf-158A': 'Since the scores are sorted in non-increasing order, look up the score at the (k-1)-th index (0-indexed). Count how many participants scored at least this threshold and have a score strictly greater than 0. Time complexity: O(n), Space complexity: O(1).',
  'cf-263A': 'The matrix is 5x5. Find the 1-based indices (r, c) of the single "1" in the matrix. The minimum number of moves is the Manhattan distance to the center (3,3), calculated as |r - 3| + |c - 3|. Time complexity: O(1), Space complexity: O(1).',
  'cf-1A': 'The flagstones cannot be broken and must be parallel to the sides. The number of flagstones needed along the length is ceil(n/a) and along the width is ceil(m/a). Total needed is ceil(n/a) * ceil(m/a). Use long long to avoid 32-bit overflow. Time complexity: O(1), Space complexity: O(1).',
  'cf-112A': 'Convert both strings to lowercase. Perform a lexicographical comparison. If s1 < s2 print -1, if s1 > s2 print 1, else print 0. Time complexity: O(n), Space complexity: O(1) auxiliary.',
  'cf-339A': 'Extract the digits (1, 2, and 3) from the expression, sort them in non-decreasing order, and join them back using plus signs. Time complexity: O(n log n) or O(n) using counting sort, Space complexity: O(n).',
  'cf-281A': 'Capitalize the first letter of the word (if not empty) and concatenate the rest of the string unchanged. Time complexity: O(n) to copy the string, Space complexity: O(1) auxiliary.',
  'cf-266A': 'Iterate through the string from index 1. If s[i] == s[i-1], we must remove one of them, so increment the count. This ensures no two adjacent stones have the same color. Time complexity: O(n), Space complexity: O(1).',
  'cf-236A': 'Insert all characters of the username into a set to find the number of distinct characters. If the size of the set is odd, print "IGNORE HIM", else print "CHAT WITH HER!". Time complexity: O(n), Space complexity: O(1) (at most 26 characters).',
  'cf-266B': 'Simulate the queue rearrangement second by second. In each second, scan from left to right: if a boy (\'B\') is standing immediately before a girl (\'G\'), swap them and advance the pointer by 2 to prevent the same boy from moving twice in the same second. Time complexity: O(n * t), Space complexity: O(1) auxiliary.',
  'lc-20': 'Use a stack to track open brackets. Iterate through the string; if it is an opening bracket, push it to the stack. If it is a closing bracket, check if it matches the top of the stack and pop it. The string is valid if the stack is empty at the end. Time complexity: O(n), Space complexity: O(n).',
  'lc-21': 'Use a dummy node to build the merged list. Compare the head nodes of both lists, attach the smaller one to the merged list, and advance the corresponding pointer. Continue until one list is exhausted, then append the remaining list. Time complexity: O(n + m), Space complexity: O(1).',
  'lc-141': 'Use Floyd\'s Cycle Detection (Tortoise and Hare). Maintain two pointers: slow moves 1 step at a time, and fast moves 2 steps. If there is a cycle, the two pointers will meet; otherwise, fast will reach the end (null). Time complexity: O(n), Space complexity: O(1).',
  'lc-206': 'Iterate through the linked list. Maintain three pointers: prev (initially null), current (initially head), and next_node. At each step, point current.next to prev, then advance prev to current and current to next_node. Return prev as the new head. Time complexity: O(n), Space complexity: O(1).',
  'lc-704': 'Use binary search. Maintain low and high pointers. At each step, calculate mid = low + (high - low)/2. If nums[mid] == target, return mid. If nums[mid] < target, search in the right half (low = mid + 1). Otherwise, search in the left half (high = mid - 1). Time complexity: O(log n), Space complexity: O(1).',
  'lc-226': 'Recursively invert the left and right subtrees, then swap the left and right pointers of the current root. Base case: if root is null, return null. Time complexity: O(n), Space complexity: O(h) where h is the tree height due to recursion stack.',
  'lc-235': 'Start at the root. Since it is a BST, if both p and q are smaller than the current node, the LCA must be in the left subtree. If both are larger, it must be in the right subtree. Otherwise, the current node is the split point, which is the LCA. Time complexity: O(h), Space complexity: O(1) iterative.',
  'lc-104': 'The maximum depth of a binary tree is 1 + the maximum of the depths of its left and right subtrees. Base case: return 0 for a null node. Time complexity: O(n), Space complexity: O(h) recursion stack.',
  'lc-11': 'Use two pointers, left (start) and right (end). The area is bounded by min(height[left], height[right]) * (right - left). Calculate area at each step, track the maximum, and move the pointer pointing to the shorter line inwards to explore potentially taller containers. Time complexity: O(n), Space complexity: O(1).',
  'lc-15': 'Sort the array first. Loop through the array, treating the current element as a fixed target. Use two pointers for the remaining part of the array to find pairs that sum to the negative of the target. Skip duplicate elements to avoid duplicate triplets. Time complexity: O(n^2), Space complexity: O(log n) to O(n) depending on the sorting implementation.',
  'lc-3': 'Use a sliding window with two pointers representing the current substring without repeating characters. Use a set to track characters in the window. Expand the right pointer. If a duplicate is found, shrink the left pointer until the duplicate is removed. Track the maximum window size. Time complexity: O(n), Space complexity: O(min(m, n)) where m is alphabet size.',
  'lc-36': 'Use a set to track visited values in each row, column, and 3x3 box. Loop through the grid; for each non-empty cell board[r][c], construct unique string keys representing its presence in row r, col c, and box (r/3, c/3). If any key is already in the set, the board is invalid. Time complexity: O(1) since board is always 9x9, Space complexity: O(1).',
  'lc-22': 'Use backtracking. Keep track of the number of open and closed parentheses used. We can add an open parenthesis if open_count < n, and we can add a closed parenthesis if closed_count < open_count. When both counts reach n, add the combination to the result. Time complexity: O(4^n / sqrt(n)) Catalan number of steps, Space complexity: O(n) recursion stack.',
  'lc-33': 'Perform binary search. Even after rotation, one half of the search range is always sorted. Determine which half is sorted by comparing nums[low] and nums[mid]. Then check if the target lies within the sorted half to decide whether to search left or right. Time complexity: O(log n), Space complexity: O(1).',
  'lc-46': 'Use backtracking. Maintain a list of used elements. In each recursive call, iterate through the numbers; if not used, add to the current permutation path, mark as used, recurse, and then backtrack by removing the element and marking it as unused. Time complexity: O(n * n!), Space complexity: O(n) recursion stack.',
  'lc-56': 'Sort intervals by their start times. Iterate through the sorted intervals; if the current interval starts after the last merged interval ends, append it as a new interval. Otherwise, merge them by updating the end time of the last merged interval to the maximum end time. Time complexity: O(n log n), Space complexity: O(log n) or O(n) for sorting.',
  'lc-49': 'Group strings by their sorted character representations. Use a hash map where the key is the sorted string (or a character count array) and the value is a list of anagrams. Return the values of the map. Time complexity: O(n * k log k) where k is the maximum string length, Space complexity: O(n * k).',
  'lc-102': 'Perform BFS (Level Order Traversal) using a queue. At each level, record the size of the queue. Loop through that size to process all nodes of the current level, adding their values to a list and pushing their children to the queue. Time complexity: O(n), Space complexity: O(n).',
  'lc-215': 'Use a min-heap of size k. Push elements into the heap. If the heap size exceeds k, pop the smallest element. After processing all numbers, the root of the min-heap will be the kth largest element. Time complexity: O(n log k), Space complexity: O(k).',
  'lc-207': 'Represent prerequisites as a directed graph. Detect if a cycle exists in the graph using DFS with a recursion stack / visiting set (or Kahn\'s BFS indegree algorithm). If a cycle is detected, we cannot finish all courses. Time complexity: O(V + E), Space complexity: O(V + E).',
  'lc-200': 'Traverse the grid. When a \'1\' (land) is found, increment the island count and perform DFS or BFS to visit and sink (mark as \'0\') all connected land cells. Time complexity: O(r * c), Space complexity: O(r * c) worst-case recursion stack.',
  'lc-322': 'Use dynamic programming. Maintain a 1D DP table where dp[i] represents the minimum number of coins to make up amount i. Transition: dp[i] = min(dp[i], dp[i - coin] + 1) for each coin. Initialize dp[0] = 0 and others to infinity. Time complexity: O(n * amount), Space complexity: O(amount).',
  'lc-198': 'Use DP. Let dp[i] be the max money robbed from the first i houses. Since we cannot rob adjacent houses, the decision at house i is: rob it (nums[i] + dp[i-2]) or skip it (dp[i-1]). Optimize space by tracking only the last two values. Time complexity: O(n), Space complexity: O(1).',
  'lc-79': 'Use backtracking with DFS. From each cell, try to match the word characters by moving in four directions. Mark the current cell as visited (e.g., using a temp character) to avoid reusing it, then restore it after backtracking. Time complexity: O(m * n * 4^L) where L is word length, Space complexity: O(L) recursion stack.',
  'lc-78': 'Use backtracking. Generate subsets by either choosing to include or exclude the current element at each index. Alternatively, iterate through the numbers and add the current number to all existing subsets to generate new ones. Time complexity: O(n * 2^n), Space complexity: O(n) recursion stack.',
  'lc-739': 'Use a monotonic decreasing stack storing indices. Iterate through the temperatures. If the current temperature is greater than the temperature at the index on top of the stack, pop the index and calculate the difference in days. Push current index to stack. Time complexity: O(n), Space complexity: O(n).',
  'lc-208': 'Implement a Trie (Prefix Tree) where each node contains an array/map of size 26 for children and a boolean flag isEnd. Insert adds characters node by node. Search and startsWith traverse the trie according to character keys. Time complexity: O(L) per operation, Space complexity: O(total_chars).',
  'lc-155': 'Use two stacks: a main stack to store all elements, and a min stack to track the minimum element at each stage. When pushing val, push val to the min stack if it is empty or <= current minimum. Time complexity: O(1) for all operations, Space complexity: O(n).',
  'lc-4': 'Use binary search to partition the two arrays. Find a partition point in nums1 and nums2 such that the left half has the same size as the right half, and all elements in the left half are <= all elements in the right half. Time complexity: O(log(min(m, n))), Space complexity: O(1).',
  'lc-23': 'Merge k sorted lists using a min-heap (priority queue). Push the head of each non-empty list into the heap. Repeatedly pop the smallest node, append it to the merged list, and push its next node into the heap. Time complexity: O(n log k) where n is total nodes, Space complexity: O(k).',
  'lc-32': 'Use a stack to store indices. Initialize the stack with -1 to handle base cases. Iterate through the string; if \'(\', push its index. If \')\', pop the top index. If stack is not empty, update max length as current_index - stack_top. If empty, push current index. Time complexity: O(n), Space complexity: O(n).',
  'lc-239': 'Use a monotonic deque storing indices of elements in decreasing order. For each index i, remove indices out of the window (index < i - k + 1). Remove elements from the back of the deque that are smaller than nums[i]. Add i to deque. The front of the deque is the max element index. Time complexity: O(n), Space complexity: O(k).',
  'lc-72': 'Use 2D DP. Let dp[i][j] be the edit distance between word1[0..i-1] and word2[0..j-1]. Transition: if word1[i-1] == word2[j-1], dp[i][j] = dp[i-1][j-1]. Otherwise, dp[i][j] = 1 + min(dp[i-1][j] (delete), dp[i][j-1] (insert), dp[i-1][j-1] (replace)). Time complexity: O(m * n), Space complexity: O(m * n).',
  'lc-127': 'Use BFS. Start from beginWord and find all valid single-character transformations in wordList. Use a set for O(1) lookup of wordList. Perform level-order traversal, incrementing path length at each level. Return path length when endWord is reached. Time complexity: O(n * L^2) where L is word length, Space complexity: O(n * L).',
  'lc-42': 'Use two pointers, left and right, with leftMax and rightMax variables. Move the pointer with the smaller max height inward, adding the trapped water at that cell (max_height - height[i]). Time complexity: O(n), Space complexity: O(1).',
  'lc-51': 'Use backtracking. Place queens row by row. Maintain sets for columns, positive diagonals (r + c), and negative diagonals (r - c) to check safety in O(1) time. Backtrack if no valid placement exists in a row. Time complexity: O(n!), Space complexity: O(n).',
  'lc-10': 'Use 2D DP or memoized recursion. Let dp[i][j] represent whether s[i..] matches p[j..]. Handle the \'*\' wildcard by checking zero occurrences of the preceding character (dp[i][j+2]) or matching one character and moving forward in s (if characters match). Time complexity: O(m * n), Space complexity: O(m * n).',
  'lc-269': 'Represent the alphabet order as a directed graph. Build the graph by comparing adjacent words in the sorted dictionary to find the first differing characters. Perform topological sort (DFS or Kahn\'s) and check for cycles. Return empty string if a cycle exists. Time complexity: O(total_chars), Space complexity: O(1) since alphabet size is bounded.'
};

function main() {
  const arena = JSON.parse(fs.readFileSync(ARENA_PATH, 'utf-8'));
  let updated = 0;

  for (const problem of arena) {
    if (EXPLANATIONS[problem.id]) {
      problem.solutions.explanation = EXPLANATIONS[problem.id];
      updated++;
    }
  }

  fs.writeFileSync(ARENA_PATH, JSON.stringify(arena, null, 2) + '\n', 'utf-8');
  console.log(`Updated explanations for ${updated} problems.`);
}

main();
